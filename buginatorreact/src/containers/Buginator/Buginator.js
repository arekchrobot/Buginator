import React from "react";
import Auth from '../../components/Auth/Auth';

const buginator = () => {
    return (
        <div className="login">
            <Auth />
        </div>
    );
}

export default buginator;